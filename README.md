 Invoice-Generator-
I built an invoice generator using React and TypeScript. It handles dynamic invoice items, calculates totals in real time, and uses TypeScript interfaces to ensure type safety.

Invoice Generator

A simple invoice generator built with React and TypeScript.

Features
- Add multiple invoice items
- Automatic total calculation
- Strong typing with TypeScript
- Clean component-based structure

 Tech Stack
- React
- TypeScript
- CSS

#m Run Locally
npm install  
npm run dev
